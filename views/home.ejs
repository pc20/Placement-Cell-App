<!DOCTYPE html>
<html lang="en">

<head>
  <!-- Required Meta Tags -->
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
    integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous" />
  <link href="https://fonts.googleapis.com/css2?family=Poppins&display=swap" rel="stylesheet" />
  <title>Home</title>
</head>

<body>
  <%- include('_header') %>
    <div class="w-75 mx-auto mt-5">
      <br />

      <div class="row">
        <div class="col-6">
          <h3>Total Students: <%=students.length%>
          </h3>
        </div>

        <div class="col-6 d-flex justify-content-end">
          <a href="/students/addPage" type="button" class="btn btn-primary align-self-center">+ Add Student</a>
        </div>
      </div>

      <% if(students.length> 0) {%>
        <div class="mt-3 mb-3">
          <table class="table table-striped table-light" style="border: 1px solid black;">
            <thead class="table-secondary">
              <tr>
                <th scope="col">Batch</th>
                <th scope="col">Name</th>
                <th scope="col">Email</th>
                <th scope="col">Contact</th>
                <th scope="col">DSA</th>
                <th scope="col">WEB</th>
                <th scope="col">REACT</th>
                <th scope="col">Action</th>
              </tr>
            </thead>

            <tbody>

              <% for(let student of students) {%>
                <form action="/students/delete/<%=student.id%>" method="post">
                  <% if(student.placement=='Placed' ) {%>
                    <tr class="table-success">
                      <%}else{%>
                    <tr>
                      <%}%>
                        <th scope="row">
                          <%=student.batch%>
                        </th>
                        <td>
                          <%=student.name%>
                        </td>
                        <td>
                          <%=student.email%>
                        </td>
                        <td>
                          <%=student.contactNumber%>
                        </td>
                        <td>
                          <%=student.dsa%>
                        </td>
                        <td>
                          <%=student.webd%>
                        </td>
                        <td>
                          <%=student.react%>
                        </td>
                        <td>
                          <button class="btn btn-danger btn-sm" type="submit"> DELETE</button>
                        </td>
                    </tr>
                </form>
                <%}%>
            </tbody>
          </table>
        </div>
        <%}else{%>
          <div class="w-75 mx-auto mt-5">
            <h2>No Records Found</h2>
          </div>
          <%}%>
    </div>
    <%- include('_bootStrap_script')%>
</body>

</html>